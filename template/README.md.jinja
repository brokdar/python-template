# {{ project_name }}

{{ description }}

## Prerequisites

- Python {{ python_version }}+
- [uv](https://docs.astral.sh/uv/)

## Getting Started

```bash
# Install dependencies
uv sync

# Run the application
uv run python -m {{ package_name }}.main

# Run tests
uv run pytest
```

## Development

### Code Quality

```bash
# Linting
uv run ruff check .

# Formatting
uv run ruff format .

# Type checking
uv run {{ type_checker }}{% if type_checker == "pyrefly" %} check{% endif %} .

# Run all checks (via pre-commit)
uv run pre-commit run --all-files
```

### Testing

```bash
# Run tests
uv run pytest

# With coverage
uv run pytest --cov={{ package_name }}

# Verbose output
uv run pytest -v
```
{% if include_dockerfile %}

### Docker

```bash
# Build image
docker build -t {{ project_slug }} .

# Run container
docker run --rm {{ project_slug }}
```
{% endif %}

## Project Structure

```text
{{ package_name }}/
├── __init__.py
├── main.py          # Application entry point
├── logging.py       # Structured logging (structlog)
└── py.typed         # PEP 561 type marker

tests/
└── test_main.py     # Test suite
```
{% if license != "None" %}

## License

[{{ license }}](LICENSE)
{% endif %}
